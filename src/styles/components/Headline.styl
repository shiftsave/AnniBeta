.Headline
  center()
  min-height: 100vh
  padding $xxl 0 0
  position: relative

  .TextArea
    position: relative;

    .ButtonGroup
      position: absolute;
      right: -($xxl)
      top: 50%
      transform: translateY(-50%);

  .date
    margin $xl 0 0
